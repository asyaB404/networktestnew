using FishNet.Transporting;

public class RoomPanel : BasePanel<RoomPanel>
{
    public override void Init()
    {
        base.Init();
        // NetworkMgr.Instance.networkManager.ClientManager
    }
}